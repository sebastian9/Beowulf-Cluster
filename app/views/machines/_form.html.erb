<%= form_with(model: machine, local: true) do |form| %>
  <% if machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(machine.errors.count, "error") %> prohibited this machine from being saved:</h2>

      <ul>
      <% machine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :IP %>
    <%= form.text_field :IP, id: :machine_IP %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :machine_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
